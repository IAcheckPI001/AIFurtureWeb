
services:
  frontend:
    build: ./
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8000

  db:
    image: postgres:17
    restart: always
    container_name: postgres_db
    environment:
      POSTGRES_USER: cus_sys01
      POSTGRES_PASSWORD: cus01#
      POSTGRES_DB: AIFurture
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
      

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.1
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    ports:
      - "9200:9200"
    volumes:
      - esdata:/usr/share/elasticsearch/data

  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - db
      - elasticsearch

    
volumes:
  pgdata:
  esdata: